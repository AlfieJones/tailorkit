---
"@tailorkit/app": patch
"@tailorkit/cli": patch
"@tailorkit/client-platform": patch
"@tailorkit/core": patch
"@tailorkit/react": patch
---

Add optional light and dark app logos to the deployment pipeline, including validated SVG, PNG, and WebP uploads, hosted logo URLs, and a 1 MiB combined client asset limit.
