---
"@tailorkit/cli": patch
"@tailorkit/client-platform": patch
"@tailorkit/core": patch
---

Serve preview assets directly through the authenticated tunnel without starting a local HTTP server, and end preview sessions when the CLI exits or cannot establish its tunnel.
