---
"@tailorkit/client-platform": patch
"@tailorkit/core": patch
---

Expose platform-managed signed app bundle URLs in registry responses. Hosted apps do not require an assetsBaseUrl override or direct access to the platform's storage provider.
