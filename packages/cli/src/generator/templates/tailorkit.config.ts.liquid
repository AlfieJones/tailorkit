import type { TailorKitConfig } from "@tailorkit/app/config";

const config = {
  host: "{{ hostUrl }}",
  client: {
    entry: "./src/client.ts",
  },
} satisfies TailorKitConfig;

export default config;
