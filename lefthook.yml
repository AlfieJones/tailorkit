# Lefthook configuration
# https://github.com/evilmartians/lefthook
pre-commit:
  parallel: false
  jobs:
    - name: oxlint
      run: bun oxlint --fix {staged_files} || true
      stage_fixed: true

    - name: oxfmt
      run: bun oxfmt --write {staged_files} || true
      stage_fixed: true
