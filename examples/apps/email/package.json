{
  "name": "email",
  "private": true,
  "description": "A TailorKit.dev app that embeds in a host app to extend what it can do.",
  "type": "module",
  "imports": {
    "#tailorkit": "./src/tailorkit.gen.ts",
    "#*": [
      "./src/*",
      "./src/*.ts",
      "./src/*.tsx",
      "./src/*/index.ts",
      "./src/*/index.tsx"
    ]
  },
  "scripts": {
    "build": "tailorkit build",
    "generate": "tailorkit generate --experimental-schema-file ../../shared/src/index.ts",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt --check",
    "format:fix": "oxfmt --write",
    "check": "pnpm run lint && pnpm run format",
    "fix": "pnpm run lint:fix && pnpm run format:fix"
  },
  "dependencies": {
    "@tailorkit/app": "workspace:*",
    "preact": "^10.29.2"
  },
  "devDependencies": {
    "@tailorkit/cli": "workspace:*",
    "oxfmt": "^0.51.0",
    "oxlint": "^1.66.0",
    "typescript": "^6.0.3"
  }
}
