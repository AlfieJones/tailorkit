---
title: Deploying to production
description: Ship TailorKit apps with clear host-owned security boundaries.
---

import { Callout } from "fumadocs-ui/components/callout";

TailorKit apps run in a sandbox. That helps, but it does not make every value
safe automatically.

## Trust Boundaries

TailorKit sandboxes app JavaScript for you, so embedded apps cannot directly
reach into your page or application state. Your host app still decides which
fields, callbacks, and actions are available.

The production rule is simple: expose the pieces an app needs, validate the
values it sends back, and keep side effects limited to the work your app
expects.

## Validate Fields

Keep schemas narrow. Prefer enums, bounded numbers, booleans, and structured
objects over broad strings.

### Links And Sources

<Callout title="Validate links">
  Do not render arbitrary strings as `href` or `src` values. Accept only safe protocols, usually
  `http:` and `https:`.
</Callout>

```ts title="./src/lib/tailor.ts" tab="Zod"
import { component } from "tailorkit";
import { z } from "zod";

const Link = component({
  fields: z.object({
    href: z.url(),
  }),
});
```

```ts title="./src/lib/tailor.ts" tab="ArkType"
import { type } from "arktype";
import { component } from "tailorkit";

const Link = component({
  fields: type({
    href: "string.url",
  }),
});
```

```ts title="./src/lib/tailor.ts" tab="Valibot"
import { component } from "tailorkit";
import * as v from "valibot";

const Link = component({
  fields: v.object({
    href: v.pipe(v.string(), v.url()),
  }),
});
```

### Text And HTML

Treat long-form text as text, not markup. Avoid accepting raw HTML unless you
sanitize it before rendering.

## Keep Callbacks UI-Only

### UI Events

Callbacks are for UI events like `onClick`, `onOpenChange`, and
`onValueChange`. Do not use them to call APIs, mutate records, or start billing
or auth flows.

### Trusted Actions

For trusted work, expose a host-owned [action](/docs/actions).

## Constrain Styling

### Styles And Classes

Avoid arbitrary `style` objects, raw class names, and HTML. Prefer
[TailorKit primitives](/docs/layouts-and-styles) and theme tokens so apps can
feel native without controlling layout, positioning, or visual hierarchy.

### Sensitive UI

<Callout title="Keep trusted UI recognizable">
  Destructive, billing, auth, invite, role, and permission controls should look and behave like
  host-owned UI. Do not let an embedded app visually replace those flows.
</Callout>

## Production Checklist

Before deploying:

<div className="not-prose mt-4 grid gap-3">
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>
      Validate fields with narrow schemas such as enums, bounded numbers, booleans, and structured
      objects.
    </span>
  </label>
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>
      Validate every link <code>href</code> and media <code>src</code> before rendering it.
    </span>
  </label>
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>
      Limit URLs to expected protocols, usually <code>http:</code> and <code>https:</code>.
    </span>
  </label>
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>Treat user-provided text as text, not raw HTML.</span>
  </label>
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>
      Keep callbacks limited to UI events such as <code>onClick</code>, <code>onOpenChange</code>,
      and <code>onValueChange</code>.
    </span>
  </label>
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>
      Use actions for API calls, record mutations, billing, auth, invites, role changes, and other
      side effects.
    </span>
  </label>
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>
      Avoid arbitrary <code>style</code> objects unless each allowed property and value is
      constrained.
    </span>
  </label>
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>Avoid raw class names; prefer TailorKit primitives and theme tokens.</span>
  </label>
  <label className="flex items-start gap-3 rounded-md border bg-card p-3 text-sm leading-6">
    <input className="mt-1" type="checkbox" />
    <span>
      Keep destructive, billing, auth, invite, role, and permission controls visually recognizable
      as trusted UI.
    </span>
  </label>
</div>
